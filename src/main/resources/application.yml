server:
  port: 8081

spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/dynamic-shed-lock
    username: root
    password: Maiyeuem@123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true